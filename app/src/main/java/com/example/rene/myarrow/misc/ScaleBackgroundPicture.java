package com.example.rene.myarrow.misc;

/**
 * Created by rened on 02.03.2016.
 */
public class ScaleBackgroundPicture {
/*
    private Point size = new Point();
    private int mPicture;
    private Context mContext;

    public ScaleBackgroundPicture(Context context){
        mContext = context;
        // adapt the image to the size of the display
        WindowManager windowManager = (WindowManager) mContext
                .getSystemService(Context.WINDOW_SERVICE);
        Display display = windowManager.getDefaultDisplay();
        display.getSize(size);
    }

    public void setPicture(int pictureID){
        mPicture = R.drawable.background;
    }

    public void setBackground() {

        // Bild skalieren
        Bitmap bmp = Bitmap.createScaledBitmap(BitmapFactory.decodeResource(
                mContext.getResources(), mPicture), size.x, size.y, true);

        // fill the background ImageView with the resized image
        ImageView iv_background = (ImageView) findViewById(R.id.iv_background);
        iv_background.setImageBitmap(bmp);
    }
*/
}
